# Slice 228: Markdown Projected Child Review Groups

Markdown fenced code delegation MAY project more than one child review case for
one fenced block.

When that happens, grouped projection SHOULD preserve the fenced block as the
shared delegated apply group so hosts can replay or present those child cases
as one child-session unit.
