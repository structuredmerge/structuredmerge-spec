# Slice 218: Ruby Analysis

Defines the initial Ruby family owner analysis contract.

Portable owners in this slice are:

- `require`
- `declaration`

The contract is intentionally shallow. It is sufficient to support matching and
surface ownership without claiming full Ruby AST merge semantics.
