# Slice 664: Structured Edit Provider Execution Receipt Replay Workflow Apply Decision Closure Report Envelope Application

## Goal

Apply one supported replay-workflow apply-decision closure-report envelope.

## Shared Behavior

This slice defines one shared receipt replay-workflow apply-decision
closure-report envelope application contract:

1. a supported closure-report envelope imports to the same closure-report
   record unchanged,
2. rejected envelopes preserve the slice-663 rejection behavior.

## Notes

- This slice completes the single CRISPR-facing replay-workflow
  apply-decision closure-report transport line.
