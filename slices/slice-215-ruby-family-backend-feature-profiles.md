# Slice 215: Ruby Family Backend Feature Profiles

Defines the Ruby family substrate backend feature profile.

- backend: `kreuzberg-language-pack`
- family: `tree-sitter`
- supports dialects: `true`

This slice is intentionally substrate-scoped. Native one-trick parser support is
carried by provider gems such as `prism-merge`.
