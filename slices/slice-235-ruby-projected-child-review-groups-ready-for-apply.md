# Slice 235: Ruby Projected Child Review Groups Ready For Apply

`ruby-merge` MUST apply the shared projected-child ready-for-apply helper to
delegated child review groups discovered from Ruby documentation surfaces, such
as YARD example blocks.

This proves that fully resolved child groups inside comment-owned surfaces can
be applied without flattening or collapsing incomplete sibling groups.
