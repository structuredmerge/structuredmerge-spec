# Slice 632: Structured Edit Provider Execution Receipt Replay Workflow Apply Decision Envelope Application

## Goal

Apply one supported replay-workflow apply-decision envelope.

## Shared Behavior

This slice defines one shared receipt replay-workflow apply-decision envelope
application contract:

1. a supported replay-workflow apply-decision envelope imports to the same
   replay-workflow apply-decision record unchanged,
2. rejected envelopes preserve the slice-631 rejection behavior.

## Notes

- This slice completes the single CRISPR-facing replay-workflow apply-decision
  transport line.
