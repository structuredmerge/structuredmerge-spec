# Slice 648: Structured Edit Provider Execution Receipt Replay Workflow Apply Decision Settlement Envelope Application

## Goal

Apply one supported replay-workflow apply-decision settlement envelope.

## Shared Behavior

This slice defines one shared receipt replay-workflow apply-decision
settlement envelope application contract:

1. a supported replay-workflow apply-decision settlement envelope imports to
   the same replay-workflow apply-decision settlement record unchanged,
2. rejected envelopes preserve the slice-647 rejection behavior.

## Notes

- This slice completes the single CRISPR-facing replay-workflow
  apply-decision settlement transport line.
