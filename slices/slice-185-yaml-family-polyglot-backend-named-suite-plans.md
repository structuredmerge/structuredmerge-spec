# Slice 185: YAML Polyglot Backend Named-Suite Plans

## Goal

Plan `yaml_portable` through the `tree-haver` polyglot YAML backend path.

## Shared Behavior

This slice defines one YAML polyglot named-suite planning contract:

1. `yaml_portable` remains the same suite definition,
2. the selected YAML backend becomes `kreuzberg-language-pack`,
3. the plan still resolves the same YAML analysis, matching, and merge fixture
   paths.
