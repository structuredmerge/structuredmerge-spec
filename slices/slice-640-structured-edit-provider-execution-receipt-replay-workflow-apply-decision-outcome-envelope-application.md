# Slice 640: Structured Edit Provider Execution Receipt Replay Workflow Apply Decision Outcome Envelope Application

## Goal

Apply one supported replay-workflow apply-decision outcome envelope.

## Shared Behavior

This slice defines one shared receipt replay-workflow apply-decision outcome
envelope application contract:

1. a supported replay-workflow apply-decision outcome envelope imports to the
   same replay-workflow apply-decision outcome record unchanged,
2. rejected envelopes preserve the slice-639 rejection behavior.

## Notes

- This slice completes the single CRISPR-facing replay-workflow apply-decision
  outcome transport line.
