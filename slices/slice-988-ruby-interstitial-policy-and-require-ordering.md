# Slice 988: Ruby Interstitial Policy And Require Ordering

Ruby should expose interstitial merge rules separately from owner merge rules.

This slice covers two pieces:

- the Ruby interstitial policy profile;
- require/import ordering where destination order is preserved and
  template-only requires are appended.

