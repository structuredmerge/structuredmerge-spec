# Slice 994: Ruby Never-Worse Fallback Mode

Ruby should expose a "never worse than baseline" fallback mode. Structured
output must not produce more conflicts, broader conflicts, or clean-content loss
relative to the configured baseline provider.

