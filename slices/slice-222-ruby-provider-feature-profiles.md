# Slice 222: Ruby Provider Feature Profiles

Defines the native provider feature profile for `prism-merge`.

- backend: `prism`
- family: `native`

This provider shares the Ruby family contract while owning its one-trick native
parser backend locally rather than through `tree_haver`.
