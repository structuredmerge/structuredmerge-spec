# Slice 652: Structured Edit Provider Batch Execution Receipt Replay Workflow Apply Decision Settlement Envelope Application

## Goal

Apply one supported batch replay-workflow apply-decision settlement envelope.

## Shared Behavior

This slice defines one shared batch receipt replay-workflow apply-decision
settlement envelope application contract:

1. a supported batch replay-workflow apply-decision settlement envelope
   imports to the same batch replay-workflow apply-decision settlement record
   unchanged,
2. rejected envelopes preserve the slice-651 rejection behavior.

## Notes

- This slice completes the batch CRISPR-facing replay-workflow
  apply-decision settlement transport line.
