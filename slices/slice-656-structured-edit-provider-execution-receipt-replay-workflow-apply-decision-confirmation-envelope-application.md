# Slice 656: Structured Edit Provider Execution Receipt Replay Workflow Apply Decision Confirmation Envelope Application

## Goal

Apply one supported replay-workflow apply-decision confirmation envelope.

## Shared Behavior

This slice defines one shared receipt replay-workflow apply-decision
confirmation envelope application contract:

1. a supported apply-decision confirmation envelope imports to the same
   apply-decision confirmation record unchanged,
2. rejected envelopes preserve the slice-655 rejection behavior.

## Notes

- This slice completes the single CRISPR-facing replay-workflow
  apply-decision confirmation transport line.
