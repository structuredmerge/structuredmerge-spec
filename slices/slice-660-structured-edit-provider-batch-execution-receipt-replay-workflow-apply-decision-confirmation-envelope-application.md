# Slice 660: Structured Edit Provider Batch Execution Receipt Replay Workflow Apply Decision Confirmation Envelope Application

## Goal

Apply one supported batch replay-workflow apply-decision confirmation
envelope.

## Shared Behavior

This slice defines one shared batch receipt replay-workflow apply-decision
confirmation envelope application contract:

1. a supported batch apply-decision confirmation envelope imports to the same
   batch apply-decision confirmation record unchanged,
2. rejected envelopes preserve the slice-659 rejection behavior.

## Notes

- This slice completes the batch CRISPR-facing replay-workflow
  apply-decision confirmation transport line.
