# Slice 184: YAML Polyglot Backend Plan Contexts

## Goal

Plan YAML conformance through the `tree-haver` polyglot backend path.

## Shared Behavior

This slice defines one YAML polyglot planning contract:

1. YAML family contexts MAY select `kreuzberg-language-pack`,
2. the resulting feature profile preserves the same YAML family policy surface,
3. semantic YAML normalization still belongs to the family package.
