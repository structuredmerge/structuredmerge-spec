# Slice 1003: Ruby tree_haver Reference Contract

Ruby `tree_haver` should expose one fixture-backed reference report proving the
parser/backend substrate needed by source-region extraction: stable spans,
source fragments, backend-restricted comments, parser diagnostics, backend
capability reports, and backend selection context.
