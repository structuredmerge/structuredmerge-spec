# Slice 993: Ruby Fallback Activation Report

Ruby should expose fallback activation as a machine-readable report with reason,
scope, selected baseline provider, and whether a structured result was
discarded.

