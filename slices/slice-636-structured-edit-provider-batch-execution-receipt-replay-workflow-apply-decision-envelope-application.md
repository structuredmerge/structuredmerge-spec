# Slice 636: Structured Edit Provider Batch Execution Receipt Replay Workflow Apply Decision Envelope Application

## Goal

Apply one supported batch replay-workflow apply-decision envelope.

## Shared Behavior

This slice defines one shared batch receipt replay-workflow apply-decision
envelope application contract:

1. a supported batch replay-workflow apply-decision envelope imports to the
   same batch replay-workflow apply-decision record unchanged,
2. rejected envelopes preserve the slice-635 rejection behavior.

## Notes

- This slice completes the batch CRISPR-facing replay-workflow apply-decision
  transport line.
