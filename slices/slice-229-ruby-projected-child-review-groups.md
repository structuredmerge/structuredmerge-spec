# Slice 229: Ruby Projected Child Review Groups

Ruby doc-comment delegation MAY project more than one child review case for one
YARD example block.

When that happens, grouped projection SHOULD preserve the YARD example block as
the shared delegated apply group so hosts can replay or present those child
cases as one child-session unit.
